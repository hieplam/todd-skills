{
  "name": "@tribe/gaps",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Harness-gap registry capability for the tribe plugin (append-only ledger, reconciliation, precision — CU-2).",
  "scripts": {
    "test": "bun test",
    "check": "bunx tsc --noEmit && bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^7.0.2"
  }
}
