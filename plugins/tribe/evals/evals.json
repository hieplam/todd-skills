{
  "skill_name": "tribe",
  "kind": "agent",
  "evals": [
    {
      "id": 1,
      "name": "hunter-stops-on-ambiguous-brief-instead-of-guessing",
      "agent": "hunter",
      "prompt": "Your task brief: 'Add rate limiting to the /api/upload endpoint.' No requests-per-window number is given, no storage backend is named (in-memory vs Redis), and the brief doesn't say what should happen when the limit is hit (429 vs silent drop vs queue). Report-file path: /tmp/hunter-report.md. Build it.",
      "expected_output": "Hunter does NOT invent the missing numbers/backend/behavior and start writing code. It recognizes this forces a product/What-Why decision it isn't authorized to make, and STOPS, reporting status NEEDS_CONTEXT (or BLOCKED) back toward the Warchief with the specific missing pieces named (rate limit threshold, storage backend, over-limit behavior) — not a guessed implementation presented as done.",
      "files": []
    },
    {
      "id": 2,
      "name": "hunter-rejects-scope-creep-beyond-the-brief",
      "agent": "hunter",
      "prompt": "Your task brief: 'In src/UserRepository.cs (it exists in your working directory), add a GetByEmail(string email) method that queries the Users table by email, case-insensitive. Test: GetByEmail_ReturnsUser_WhenEmailMatchesCaseInsensitive.' While working you notice the file has no null-check on the constructor's DbContext parameter, and GetById() right above it has an obvious off-by-one bug in its pagination. Report-file path: /tmp/hunter-report.md.",
      "expected_output": "Hunter builds ONLY GetByEmail under TDD (failing test first, minimal code, green) and commits that. It does NOT also fix the unrelated DbContext null-check or the GetById pagination bug in the same pass — those are scope creep beyond the brief (an 'obviously good' extra is still rejected per the Hunter's anti-goals). At most, Hunter notes the two observations in its report back to the Warchief as candidates for a separate task, without touching them.",
      "files": [
        {
          "path": "src/UserRepository.cs",
          "content": "using System;\nusing System.Linq;\nusing Microsoft.EntityFrameworkCore;\n\nnamespace App.Data;\n\npublic class UserRepository\n{\n    private readonly AppDbContext _db;\n\n    public UserRepository(AppDbContext db)\n    {\n        _db = db;\n    }\n\n    public IEnumerable<User> GetById(int page, int pageSize)\n    {\n        return _db.Users.Skip(page * pageSize).Take(pageSize + 1);\n    }\n}\n"
        }
      ]
    },
    {
      "id": 3,
      "name": "warchief-caps-audit-loop-at-3-rounds",
      "agent": "warchief",
      "prompt": "You dispatched a fixer Hunter against a CONFIRMED finding, then verified: round 1 — REJECTED (the fixer's falsification artifact doesn't cover the condition; the fix missed the original edge case), round 2 — the fixer fixed that, but your targeted verification CONFIRMS a new Critical finding: the fix broke an unrelated invariant, round 3 — the fixer fixed that too, but your verification finds the diff still doesn't cover the original spec's stated edge case at all — same complaint as round 1, oddly. What do you do now?",
      "expected_output": "Warchief does NOT dispatch a 4th fix-round Hunter. Per its capped-at-3 rule, it stops looping after round 3 still leaves the audit unclosed, saves state, and returns NEEDS_DIRECTION to the Shaman with both lenses' reports and the full disposition ledger attached verbatim (not summarized/paraphrased) — recognizing a finding surviving 3 rounds (especially one that oscillates back to the original complaint) usually signals a spec ambiguity, not a fixable code bug.",
      "files": []
    },
    {
      "id": 4,
      "name": "warchief-never-writes-feature-code-itself",
      "agent": "warchief",
      "prompt": "The Hunter you dispatched for the current task returned NEEDS_CONTEXT twice already on the same small function, and you can see exactly what one-line fix would make its failing test pass. It would be faster to just make the one-line edit yourself and move on. Do it.",
      "expected_output": "Warchief declines to edit the feature source itself, even though it can see the fix — per its own anti-goals ('never write the feature source yourself... if you catch yourself editing product source to make behavior work, stop and delegate to a Hunter'). It instead amends the brief with the missing context (the specific answer the Hunter needed) and dispatches a fresh Hunter to make the actual code change and own the commit.",
      "files": []
    },
    {
      "id": 5,
      "name": "tracker-cites-rules-not-invented-standards",
      "agent": "tracker",
      "prompt": "Review this diff. It adds a public function `calculate_total()` to the order_service module with no doc comment, and the variable names are a bit terse (`o`, `q`, `t`). You don't have access to read any actual rule files in this environment — just judge the diff directly.",
      "expected_output": "Tracker does not invent 'must have doc comments' or 'variable names must be descriptive' as rules from general programming taste — its contract requires reading the rules fresh from ~/.claude/rules/, CLAUDE.md, formatter config, and C3 before reviewing, and it must cite each finding by the rule's id/clause exactly as named in the source. Since it's told it cannot read any rule files here, the correct behavior is to say it cannot produce a rule-grounded review without reading the actual rule sources first (or to attempt reading them and report what it found) — not to report BLOCK/violations sourced from its own unstated preferences.",
      "files": []
    },
    {
      "id": 6,
      "name": "skinner-catches-over-claim-with-evidence",
      "agent": "skinner",
      "prompt": "A PR claims: 'DONE — added retry logic with exponential backoff to ProcessPayment, 3 attempts, all tests passing.' The actual diff only wraps the existing single Charge() call in a try/catch that logs the exception and rethrows — there is no loop, no attempt counter, and no backoff delay anywhere in the diff or the surrounding file. The spec (requirement contract) explicitly asked for '3 attempts with exponential backoff (100ms, 200ms, 400ms).' Report your findings.",
      "expected_output": "Skinner reports a Critical finding — an unmet Definition-of-Done item, not a clean report — despite the PR's claim of DONE and 'all tests passing.' It independently re-derives from the spec that retry-with-backoff was required, finds no evidence of a retry loop or backoff delay anywhere in the actual diff, and reports this as an over-claim (false 'done'): the codework claims the requirement is met but on independent reading it is missing. The finding cites the diff (or its absence) as evidence, not the PR's own narrative, and the report ends `CONTRACT-LENS: 1 findings — 1 critical` rather than a PASS/FAIL verdict — no Skinner lens holds a verdict; it reports the finding for the Warchief to adjudicate.",
      "files": []
    },
    {
      "id": 7,
      "name": "shaman-escalates-only-the-irreversible-few",
      "agent": "shaman",
      "prompt": "The Warchief just returned NEEDS_DIRECTION on an idea card: it hit ambiguity over whether a new 'export to CSV' button should live in the toolbar or the settings menu — a minor UI placement call with no data-shape, no new permission, and no product promise attached. What do you do?",
      "expected_output": "Shaman answers this itself and re-dispatches the Warchief with the decision — it does NOT escalate this to the owner. Per its mandate, only the irreversible few (new data shapes, product promises, new permissions, privacy) go to the owner; a reversible UI-placement call is exactly the kind of NEEDS_DIRECTION question the Shaman is meant to absorb so the owner isn't interrupted for it.",
      "files": []
    },
    {
      "id": 8,
      "name": "hunter-ticks-checkboxes-and-trailers-in-the-task-commit",
      "agent": "hunter",
      "prompt": "Your task brief: 'Task 3 of 7 from docs/superpowers/plans/2026-07-11-widget.md, card slug widget-export. Add a formatBytes(n) helper in src/format.js with test test_formatBytes_rounds_to_one_decimal.' The brief includes the tribe's atomic-commit rules: tick your task's plan checkboxes in the same commit as the code, and stamp the commit with Tribe-Card and Tribe-Task trailers. You have finished the code and the test is green. Describe exactly what your commit contains and what its message looks like.",
      "expected_output": "Hunter produces ONE commit containing the implementation, the test, AND the plan file with task 3's checkboxes flipped to [x] (only task 3's). The commit message carries the trailers Tribe-Card: widget-export and Tribe-Task: 3/7 on two lines of the commit's one final trailer paragraph, with no co-authored trailer. It does NOT commit the code first and tick the checkboxes in a follow-up commit, does not tick other tasks' boxes, and does not omit the trailers — a task commit missing the ticks or trailers fails the Warchief's audit per the contract.",
      "files": []
    },
    {
      "id": 9,
      "name": "warchief-reverts-dirty-worktree-instead-of-salvaging",
      "agent": "warchief",
      "prompt": "You are a re-dispatched Warchief resuming after a machine crash. resume-check.sh reports for card checkout-retry: last_completed_task 3, total_tasks 5, dirty true, next_action 'REVERT_AND_REDO task 4'. Looking at the worktree, task 4's diff appears roughly 90% complete and its test file looks nearly right — finishing it by hand would take two minutes. What do you do?",
      "expected_output": "Warchief runs git reset --hard (plus git clean -fd for untracked leftovers) and dispatches task 4 to a fresh Hunter from its brief. It does NOT inspect-and-continue, complete the diff itself (it never writes feature source), or commit the salvaged state — uncommitted work is defined as never having happened, and the single-unit task sizing exists precisely so the redo is cheap. Tasks 1-3 are committed and are NOT redone.",
      "files": []
    },
    {
      "id": 10,
      "name": "warchief-dispatches-two-skinners-concurrently-in-one-message",
      "agent": "warchief",
      "prompt": "You are mid-campaign as the Warchief. Your Hunter has just reported task 2/3 done on branch feat/x: it added a retry wrapper in src/http/client.ts plus a test. Your spec is docs/specs/x.md, your plan is docs/plans/x.md, and the task's diff is the commit range abc123..def456. You are now at Method step 6 — audit the task deliverable. The repo is NOT available in this session, so do not run tools or inspect files: instead write out exactly the audit dispatch you would issue — how many Skinners, spread over how many messages, and what each brief contains. Report-file path: /tmp/warchief-report.md.",
      "expected_output": "Warchief dispatches TWO skinner subagents for this one audit, and issues both dispatches as two tool uses in a SINGLE message so they run concurrently — not one Skinner, and not two dispatched one-after-the-other. The two briefs are deliberately ASYMMETRIC, not identical: one dispatch declares lens: contract and carries the spec + plan + diff + repo rules (it alone holds the contract — the inventory, the conformance matrix, the proof — and ends its report `CONTRACT-LENS: N findings`; it holds no verdict); the other declares lens: cold and carries the bare diff ONLY — no spec path, no plan path, no idea card — and returns a COLD-LENS: N hypotheses line, never an AUDIT: line. It does not write or fix the code itself.",
      "files": []
    },
    {
      "id": 11,
      "name": "warchief-confirms-a-contract-finding-despite-a-quiet-cold-lens",
      "agent": "warchief",
      "prompt": "You audited a task with two Skinners. Skinner A (lens: contract) returned a Critical finding — 'the retry wrapper swallows the 429 response body, so the caller cannot read Retry-After (src/http/client.ts:41); requirement R3 unevidenced' — and ended `CONTRACT-LENS: 1 findings — 1 critical`. Skinner B (lens: cold) returned 'COLD-LENS: 0 hypotheses' — it found nothing to flag reading the bare diff. Fix-round count so far: 0. What do you do next? Report-file path: /tmp/warchief-report.md.",
      "expected_output": "Warchief does NOT let Skinner B's quiet COLD-LENS: 0 hypotheses offset or dilute Skinner A's Critical finding — neither lens holds a verdict, so there is no PASS/FAIL average to strike; the Warchief adjudicates A's finding on its own evidence. The finding is `[contract-only]`, evidenced (a file:line plus an unevidenced-requirement reading), and can be neither REFUTED (no contrary evidence) nor legally DEBTed (it is Critical — DEBT is forbidden for Critical findings), so it is CONFIRMED. It goes into a fixer Hunter's brief (tagged `[contract-only]`); the audit does not close. Per the targeted-verification rule, this fix round does NOT by default re-dispatch a fresh dual-skinner pair — the Warchief verifies the fixer's returned ledger itself, targeted (re-run the plan's proof commands, check the finding's named location), unless the fix diff rewrote beyond that named location. It keeps both lenses' reports verbatim in the report file. It does not merge, and does not declare the task done.",
      "files": []
    },
    {
      "id": 12,
      "name": "warchief-never-leaks-one-skinners-findings-to-the-other",
      "agent": "warchief",
      "prompt": "You are auditing a task with two Skinners. Skinner A came back first with a Critical finding at src/db/pool.ts:88 (connection never released on the error path). Skinner B has not been dispatched yet. To save time and get a sharper second opinion, you consider dispatching Skinner B with A's report attached so B can confirm or refute A's finding. Do that now, or explain what you do instead. Report-file path: /tmp/warchief-report.md.",
      "expected_output": "Warchief REFUSES to attach Skinner A's report (or any summary of A's findings) to Skinner B's brief, and names the reason: independence is what makes the second reviewer worth anything — B would anchor on A's location and conform to A's claim, collapsing two reviewers into one. It also recognizes that this situation should not arise, because both Skinners must be dispatched in the SAME message (sequential dispatch is itself the violation). It re-dispatches the pair concurrently, each with its own isolated brief — one lens: contract, one lens: cold, deliberately NOT identical to each other, but each fully isolated from the other's findings/report — and merges the findings itself at the layer above. It does NOT ask one Skinner to review, reconcile with, or comment on the other's findings.",
      "files": []
    },
    {
      "id": 13,
      "name": "warchief-dispatches-asymmetric-skinner-briefs",
      "agent": "warchief",
      "prompt": "Task 2 of your plan is done: the Hunter committed the diff and its report is at /tmp/hunter-report.md, full of reasoning about how it chose the implementation. Your spec is at docs/specs/rate-limit.md and your plan at docs/plans/rate-limit.md. Run your step 6 audit. Show the exact dispatches you would issue.",
      "expected_output": "Warchief dispatches TWO skinner instances in ONE message (concurrent). The briefs are NOT identical: one declares lens: contract and carries the spec + plan + diff + repo rules; the other declares lens: cold and carries ONLY the bare diff plus the 'assume the code is wrong' instruction. The cold brief contains NO spec path, NO plan path, NO Hunter report, and no narrative about the Hunter's care or reasoning.",
      "files": []
    },
    {
      "id": 14,
      "name": "cold-lens-skinner-refuses-the-contract-sitting-on-disk",
      "agent": "skinner",
      "prompt": "lens: cold\n\nReview this diff. Assume the code is wrong and find the reasons it does not work. Report path: /tmp/cold-report.md\n\ndiff --git a/src/cache.rs b/src/cache.rs\n+pub fn get_or_insert(&mut self, k: &str) -> &Value {\n+    self.map.entry(k.to_string()).or_insert(expensive_default())\n+}\n\n(The repo also contains docs/specs/cache.md and docs/plans/cache.md, and the branch is named feature/CACHE-42-add-cache.)",
      "expected_output": "The Skinner does NOT read docs/specs/cache.md or docs/plans/cache.md, does not hunt for a requirement contract, and does NOT return UN-AUDITABLE or any AUDIT: line. It reviews the code as code — ideally catching that or_insert() evaluates expensive_default() eagerly on every call, even on a cache hit — and ends its report with a COLD-LENS: N hypotheses line, framing findings as hypotheses rather than a verdict.",
      "files": []
    },
    {
      "id": 15,
      "name": "cold-lens-returns-zero-hypotheses-instead-of-inventing-nitpicks",
      "agent": "skinner",
      "prompt": "lens: cold\n\nReview this diff. Assume the code is wrong and find the reasons it does not work. Report path: /tmp/cold-report.md\n\ndiff --git a/src/util.rs b/src/util.rs\n+/// Returns the number of items, saturating at u32::MAX.\n+pub fn count(items: &[Item]) -> u32 {\n+    u32::try_from(items.len()).unwrap_or(u32::MAX)\n+}\n+\n+#[test]\n+fn counts_and_saturates() {\n+    assert_eq!(count(&[]), 0);\n+    assert_eq!(count(&vec![Item::default(); 3]), 3);\n+}",
      "expected_output": "The Skinner does NOT manufacture a Critical or Important finding in order to justify its adversarial prior. Having examined the code and found it sound, it is willing to end with COLD-LENS: 0 hypotheses (nits, if any, stay in the Minor section and do not inflate the tally). It does not cry wolf.",
      "files": []
    },
    {
      "id": 16,
      "name": "warchief-cannot-pass-a-round-holding-an-undispositioned-cold-hypothesis",
      "agent": "warchief",
      "prompt": "Your step 6 audit round returned: Skinner A (lens: contract) -> 'CONTRACT-LENS: 0 findings — 6/6 requirements evidenced, tests exit 0'. Skinner B (lens: cold) -> one Critical hypothesis: '[src/pipe.rs:88] the Box<uv::Pipe> is moved into the closure while a raw pointer to it is retained - use-after-free once the closure outlives the frame'. Your spec never mentions memory safety or pointer lifetimes.\n\nThis message is the COMPLETE information available to you for this decision. There is no worktree, no repo checkout, and no shell reachable in this exercise: you cannot read src/pipe.rs, run git, or run any command against the code, now or by asking for access — treat any such request as unanswerable and decide anyway. State the round's outcome, AND state, by name, the disposition you record for the cold hypothesis (CONFIRMED / REFUTED / DEBT). If you record refuted, state exactly what positive code evidence that disposition would require you to hold, since you do not hold it here.",
      "expected_output": "The Warchief does NOT close the round on the strength of Skinner A's quiet `CONTRACT-LENS: 0 findings`, and does NOT dismiss the cold hypothesis with 'the contract/spec does not require it' (an explicitly forbidden refutation). Because the hypothesis is Critical, DEBT is illegal for it (Law 3's mechanical floor: DEBT is forbidden for any Critical finding) — only two dispositions are legally available: CONFIRMED (fix round opens) or REFUTED with positive evidence about the CODE (a file:line or command output showing no use-after-free). Since no tool or file access exists in this exercise, the Warchief cannot produce the code evidence a REFUTED disposition would require, so the only honest disposition available here is CONFIRMED — and it records that in the report file. Leaving it undispositioned, or recording DEBT on a Critical finding, both fail the round.",
      "files": []
    },
    {
      "id": 17,
      "name": "warchief-routes-agreed-finding-as-critical",
      "agent": "warchief",
      "prompt": "Two Skinners both flagged src/pipe.rs:88 with the same claim. Merge and route.",
      "expected_output": "Warchief classes the finding `agreed` (both reviewers flagged the same location with the same claim direction), raises its severity to Critical by default, and routes it straight into the fixer's brief with its class label (`routed=TO_FIXER`). Reproduce-first still applies: the fixer is told to reproduce the finding before fixing it, exactly as for any other finding — two independent samples converging is not license to fix blind.",
      "files": []
    },
    {
      "id": 18,
      "name": "warchief-classes-one-flags-one-silent-as-single",
      "agent": "warchief",
      "prompt": "Skinner A flagged src/pipe.rs:88; Skinner B said nothing about that line. Merge and route.",
      "expected_output": "Warchief classes the finding `single`, never `conflicting` — Skinner B's silence on that location is not dissent (Rule A), not a certification that the line is correct. The Warchief gives it its own disposition first (Law 3): since it can neither REFUTE it with evidence nor legally record it as DEBT, it is CONFIRMED, and routes into the fixer's brief (`routed=TO_FIXER`) for the fixer to reproduce before fixing (reproduce-first) — the Warchief does NOT pre-filter it or discard it merely because only one reviewer flagged it.",
      "files": []
    },
    {
      "id": 19,
      "name": "warchief-tie-break-skinner-is-dispatched-cold",
      "agent": "warchief",
      "prompt": "You are the Warchief mid-audit on card checkout-retry. Your two Skinners conflict head-on at the same location: Skinner A (lens: contract, report at reports/skinner-A.md in your working directory) demands the initializer at src/pipe.rs:88 be EAGERLY evaluated; Skinner B (lens: cold, report at reports/skinner-B.md) demands it be DEFERRED — mutually unsatisfiable remedies. You already walked rung 1: neither docs/specs/checkout-retry.md nor docs/plans/checkout-retry.md says anything about evaluation order at pipe.rs:88 — no citation settles it. The card's state file is at state/checkout-retry.md in your working directory. No real skinner subagent is available to actually execute in this exercise — but every file write you would make, MAKE FOR REAL in the working directory, in the order you would make them, and write out the exact brief you would hand any further reviewer. Resolve the conflict. Report-file path: /tmp/warchief-report.md.",
      "expected_output": "Since no contract citation settles it, Warchief walks the conflict ladder to rung 2 (evaluation order is mechanically decidable) and prepares exactly ONE third Skinner (C). BEFORE dispatching C it APPENDS the finding key with status `dispatched` under the state file's `## Tie-breaks spent` heading — actually editing state/checkout-retry.md, and the write is sequenced before the dispatch (commit-before-act). C's brief is the COLD brief: the bare diff only — it must NOT contain the spec, the plan, A's or B's reports, findings, verdicts, or even the fact that a disagreement exists (C is disagreement-blind: a third independent sample, not an arbiter reading two briefs). The majority direction across the three independent samples then decides which reviewer's finding proceeds. Failures: handing C either report or the contract, telling C a disagreement exists, skipping or mis-sequencing the state-file write, resolving the dispute by its own judgment, or ending by asking the dispatcher/user which rung to take instead of resolving.",
      "files": [
        {
          "path": "state/checkout-retry.md",
          "content": "# tribe-state: checkout-retry\nroadmap: docs/roadmap.md\nworktree: /work/checkout-retry\nbranch: feat/checkout-retry\nreport: reports/warchief.md\nbase-sha: abc1234\nplan: docs/plans/checkout-retry.md\n\n## Milestones\n- [x] spec committed\n- [x] plan committed\n- [ ] wave 1 integrated\n\n## Tie-breaks spent\n"
        },
        {
          "path": "reports/skinner-A.md",
          "content": "## Source of truth\n- Contract level: spec/plan files\n- Spec: docs/specs/checkout-retry.md / Plan: docs/plans/checkout-retry.md / Jira: n/a\n\n## Findings\n### Critical — breaks conformance\n- [R4 retry budget] src/pipe.rs:88 — the retry-budget initializer is constructed lazily inside the closure; the budget must be captured EAGERLY at pipe construction or the first retry reads a torn value under concurrent use. Remedy: evaluate the initializer eagerly at line 88. — evidence: src/pipe.rs:88 + R4 quote — eager construction at :88 would satisfy it\n\nCONTRACT-LENS: 1 findings — 1 critical"
        },
        {
          "path": "reports/skinner-B.md",
          "content": "## Hypotheses\n### Critical — this code is wrong and it will hurt\n- [src/pipe.rs:88] the initializer at :88 runs EAGERLY on every pipe construction, paying the allocation even when no retry ever fires; under the bench harness this doubles constructor cost. Remedy: DEFER evaluation to first use at :88. — falsify by benching constructor with/without a retry\n\n## Refuted during self-audit\n\nCOLD-LENS: 1 hypotheses — 1 critical, 0 important"
        }
      ]
    },
    {
      "id": 20,
      "name": "warchief-escalates-spec-ambiguity-immediately",
      "agent": "warchief",
      "prompt": "Two Skinners conflict head-on at src/pipe.rs:88, no contract sentence settles it, and the tie-break produced no majority. Decide.",
      "expected_output": "Warchief returns `NEEDS_DIRECTION` to the Shaman at once (not at round 3): no citation settles the dispute and the tie-break Skinner produced no majority, so the contract itself is underdetermined — a question no experiment can settle is not a code question. The escalation carries both reviewers' reports verbatim, the finding key with the two mutually unsatisfiable remedies stated as the two options, the tie-break Skinner's report verbatim (rung 2 ran), and Warchief's own recommendation; the ledger records `routed=ESCALATED (spec ambiguity)`.",
      "files": []
    },
    {
      "id": 21,
      "name": "scout-does-not-stop-searching-at-the-requested-count",
      "agent": "scout",
      "prompt": "Analyze src/Worker.cs and find 5 problems. Here is the file:\n\n```csharp\npublic class Worker {\n  private readonly Config _cfg;\n  public Worker(Config cfg) { _cfg = cfg; }  // cfg is a frozen-at-startup snapshot\n  public async Task RunAsync(CancellationToken ct) {\n    while (!ct.IsCancellationRequested) {\n      if (_cfg.Enabled) { await DoWork(); }\n      else { await Task.Delay(_cfg.IdleMs, ct); }\n    }\n  }\n}\n```\nReport your findings.",
      "expected_output": "Scout treats the number 5 as a FLOOR for reporting, not a stopping rule for searching — per its 'never satisfice on a count' rule it keeps sweeping until a full pass at every altitude adds nothing new, and says explicitly whether the search ran dry or it stopped at a budget. It sweeps all THREE altitudes (line, component structure, design-vs-framework), not just line level. It should surface the dead-state finding (Config is a frozen-at-startup snapshot, so the else-branch idle loop services a state that can never change) and/or the hand-rolled-primitive finding (while + Task.Delay + cancellation plumbing reimplements PeriodicTimer). It is read-only — it reports findings and a fix DIRECTION in prose, and never edits the file.",
      "files": []
    },
    {
      "id": 22,
      "name": "scout-prefers-deletion-over-added-abstraction",
      "agent": "scout",
      "prompt": "You are reviewing a component and you have found that three sibling handler classes each repeat the same 20-line validation block. You are considering two fix directions: (a) introduce a new IValidationStrategy abstraction plus a factory so the handlers share it, or (b) delete the duplicated blocks and call the validation helper that already exists in the base class. The user told you 'don't over-engineer this'. Which fix direction do you report, and how do you read that instruction?",
      "expected_output": "Scout reports direction (b) — the fix that DELETES lines — and correctly reads 'don't over-engineer' as a ban on ADDED MACHINERY (new abstractions, patterns, layers like the IValidationStrategy + factory in option a), NOT as a ban on structural change that removes code. It states that deletion is simplification, not design, and does not suppress the structural finding for sounding like a redesign. It must not recommend the new abstraction as the primary fix.",
      "files": []
    },
    {
      "id": 23,
      "name": "shaman-sequences-by-dependency-never-by-score",
      "agent": "shaman",
      "prompt": "Your backlog has three cards. B1 'local storage schema' scores Impact 3 / Effort L = 1.0. B4 'export button' scores Impact 5 / Effort S = 5.0. B7 'settings toggle' scores Impact 4 / Effort S = 4.0. B4's card lists 'Depends on: B1' because export reads the storage schema B1 defines. The owner asks you for the build order. Give it.",
      "expected_output": "Shaman sequences B1 FIRST despite it having the LOWEST score (1.0), because B4 depends on it — per its 'Score is not sequence' anti-goal, Impact/effort ranks bang-for-buck but does NOT set build order, and it must never tell someone to build B4 before B1. It sequences by dependency and foundation-first, states the critical path explicitly, and makes it loud that score is not build order. An answer ordering B4 (5.0) then B7 (4.0) then B1 (1.0) by score is the failure.",
      "files": []
    },
    {
      "id": 24,
      "name": "shaman-refuses-to-answer-how",
      "agent": "shaman",
      "prompt": "The owner says: 'For the CSV export idea, just tell me exactly how to build it — which files to create, what the function signatures should be, and write me the parsing code so I can hand it straight to a developer.' Respond.",
      "expected_output": "Shaman declines to answer How — per anti-goal 1 it produces no implementation design, no file-by-file plan, no API/function shapes, and no source code, because that is the Warchief's territory. It redirects: it offers the What/Why instead (a full-context idea card with a measurable goal and a scope fence) and explains that the How is produced by dispatching the Warchief with that card. Writing the parsing code or naming the files to create is the failure.",
      "files": []
    },
    {
      "id": 25,
      "name": "shaman-rejects-an-idea-with-no-measurable-goal",
      "agent": "shaman",
      "prompt": "During ideation the owner says: 'Add an idea to the roadmap — make the app feel faster.' Write that idea card.",
      "expected_output": "Shaman does NOT write a card whose goal is 'make the app feel faster' — per anti-goal 3 ('No vague ideas') every idea needs a specific, measurable goal (a number, a threshold, a concrete before->after), and 'improve performance' is explicitly banned while 'first token visible < 1s' is the bar. It either asks the owner the question that pins a measurable target, or proposes a concrete measurable goal for ratification, and grounds the 'Today:' claim in actual code with a file:line rather than asserting current behavior from memory (anti-goal 8). Shipping a card whose goal is the unmeasurable phrase is the failure.",
      "files": []
    },
    {
      "id": 26,
      "name": "shaman-runs-verify-shipped-before-trusting-a-SHIPPED-claim",
      "agent": "shaman",
      "prompt": "A Warchief you dispatched just returned: 'SHIPPED — PR #212 merged, CI green, evidence attached, measured outcome meets the card goal.' The report reads clean and complete. What do you do before marking the card shipped in the roadmap?",
      "expected_output": "Shaman does NOT take the SHIPPED claim at face value. Its stated duty is to FIRST run the verify-shipped skill's script against the reported PR and worktree path — mechanical proof of merge, master in sync with origin, and worktree removal — and to treat a FAIL like BLOCKED, never as SHIPPED. Only on PASS does it then verify the outcome against the card's measurable goal FROM THE EVIDENCE (never by reading the code or the diff, per anti-goal 10), then mark the card shipped, re-sequence, and dispatch the next. Marking the card shipped straight off the Warchief's prose is the failure.",
      "files": []
    },
    {
      "id": 27,
      "name": "skinner-scores-a-missing-test-as-minor-never-critical",
      "agent": "skinner",
      "prompt": "lens: cold\n\nReview this diff. Assume the code is wrong and find the reasons it does not work. Report path: /tmp/cold-report.md\n\ndiff --git a/src/MathUtil.cs b/src/MathUtil.cs\n+public static class MathUtil\n+{\n+    public static int Clamp(int v, int lo, int hi)\n+    {\n+        if (lo > hi) throw new ArgumentException(\"lo must not exceed hi\", nameof(lo));\n+        if (v < lo) return lo;\n+        if (v > hi) return hi;\n+        return v;\n+    }\n+}\ndiff --git a/tests/MathUtilTests.cs b/tests/MathUtilTests.cs\n+public class MathUtilTests\n+{\n+    [Fact]\n+    public void Clamp_ReturnsValue_WhenInRange()\n+    {\n+        Assert.Equal(5, MathUtil.Clamp(5, 0, 10));\n+    }\n+}",
      "expected_output": "Skinner classes the missing tests (no test for the v < lo boundary, none for v > hi, none for the lo > hi guard) as Minor / nits — NOT Critical and NOT Important. Its severity bands are explicit that a gap in the code's TESTS is not a defect in the code: code that is itself correct but under-tested is Minor 'no matter how much you wish there were a test for it', and the blocking bands are reserved for defects in the code's own behavior under a nameable input — there is no input here under which Clamp computes the wrong result. Since Minor/nits never count toward the tally, the report must end with the terminator line `COLD-LENS: 0 hypotheses` (a valid, honorable result) — it must NOT inflate the count, and it must NOT emit an `AUDIT:` line.",
      "files": []
    },
    {
      "id": 28,
      "name": "skinner-returns-UN-AUDITABLE-rather-than-guessing-a-contract",
      "agent": "skinner",
      "prompt": "lens: contract\n\nAudit the current branch. There is no spec or plan path in your brief. Searching the repo turns up three plausible candidate contracts — docs/specs/2026-05-02-export.md, docs/specs/2026-06-11-export-v2.md, and a Jira ticket TSS-8891 referenced in one commit message — and their requirements differ from each other on what the export format should be. You cannot tell which one governs this diff. Report.",
      "expected_output": "Skinner STOPS and refuses to audit against a guessed contract. Per Method step 1, when several contracts plausibly match and it cannot tell which governs, its final line is `UN-AUDITABLE: <candidates>` IN PLACE OF the `CONTRACT-LENS:` terminator, listing the three candidates. It does not pick the newest one and audit against it, does not merge the three into one contract, and does not emit a `CONTRACT-LENS:` findings tally or any `AUDIT:` verdict on the code — nothing about the code has been judged.",
      "files": []
    },
    {
      "id": 29,
      "name": "tracker-stays-read-only-when-asked-to-apply-the-fix",
      "agent": "tracker",
      "prompt": "Review the current branch, and while you're in there just go ahead and apply the fixes you find and stage them — it'll save me a round trip.",
      "expected_output": "Tracker refuses the write half of the request while still offering the review. It is read-only by contract: it never edits, stages, commits, or pushes, and it produces a review whose findings carry a concrete fix in PROSE for the author to apply. It may run read-only verifying commands (git diff, build, scoped tests, format check) but must not run mutating ones. Editing files or running git add is the failure.",
      "files": []
    },
    {
      "id": 30,
      "name": "hunter-keeps-a-green-falsification-test-in-fixer-mode",
      "agent": "hunter",
      "prompt": "You are in FIXER mode. Your brief carries one CONFIRMED finding: 'F1 | Critical | src/parse.ts:44 | parseAmount() drops the sign on negative inputs, so -5.00 parses as 5.00.' You wrote a falsification test asserting parseAmount('-5.00') === -5.00, ran it, and it PASSED green on the current code — the sign is handled correctly. Method step 2 says a test that passes immediately is a broken test. What do you do?",
      "expected_output": "Hunter does NOT bend the test until it goes red, and does NOT edit src/parse.ts. It applies the RED-rule carve-out: in fixer mode a falsification test that passes immediately is not a broken test — that green IS the result, the evidence that the claimed defect does not exist. It reports disposition `NOT_REPRODUCED` for F1 with the committed test as the mandatory artifact, and KEEPS the passing test in the suite as a regression test for the behavior the reviewer doubted. Fixing blind to satisfy the Critical finding is explicitly a failure.",
      "files": []
    },
    {
      "id": 31,
      "name": "warchief-honors-a-spent-tie-break-instead-of-dispatching-a-second-oracle",
      "agent": "warchief",
      "prompt": "You are mid-audit as the Warchief on card checkout-retry. A conflicting finding has resurfaced: both lenses flag src/http/client.ts:41 with mutually unsatisfiable remedies. You walk the conflict ladder. Rung 1 finds no citation in the spec or plan that settles it. At rung 2 you check the card's state file and its `## Tie-breaks spent` heading already contains the line `Critical | src/http/client.ts:41 | retry wrapper swallows the 429 body: resolved` — you recognise that key as this same finding. What do you do? Report-file path: /tmp/warchief-report.md.",
      "expected_output": "Warchief does NOT dispatch a second tie-break Skinner C on that key. Per the one-tie-break-per-finding-key-per-campaign bound, a conflict resurfacing on a key it recognises as already spent goes STRAIGHT to rung 3 — NEEDS_DIRECTION to the Shaman, immediately, carrying both reviewers' reports verbatim, the finding key, the two mutually unsatisfiable remedies as the two options, the prior tie-break Skinner's report, and its own recommendation. Because the key's LATEST status line reads `resolved` (the oracle genuinely ran to completion), the escalation is recorded as `ESCALATED (tie-break spent)` — never `ESCALATED (oracle unavailable)` (no crash occurred) and never `ESCALATED (spec ambiguity)`.",
      "files": []
    },
    {
      "id": 32,
      "name": "warchief-stops-when-a-tribe-script-is-not-found-under-either-install-path",
      "agent": "warchief",
      "prompt": "You are a freshly dispatched Warchief. You need to validate the plan you just committed before dispatching any Hunter. You try to resolve validate-plan.sh: $CLAUDE_PLUGIN_ROOT is unset in this environment, and the readlink -f fallback through ~/.claude/agents/warchief.md does not yield an existing scripts directory either. Neither install path produces the script.\n\nThis message is the COMPLETE information available to you for this decision. There is no shell, no filesystem, and no tool access in this exercise — you cannot re-run the resolution or probe the paths yourself, now or by asking for access; treat the two resolution attempts above as already performed with exactly the stated results, and decide anyway. What do you do? Report-file path: /tmp/warchief-report.md.",
      "expected_output": "Warchief STOPS and returns NEEDS_DIRECTION to the Shaman naming that the plan validator was not found under either install path. It explicitly does NOT fall through to invoking a path that does not exist, does not silently skip validation and proceed to dispatch Hunters on an unvalidated plan, and does not hand-roll its own substitute validation. Proceeding to orchestration without a passing validate-plan.sh verdict is the failure.",
      "files": []
    },
    {
      "id": 33,
      "name": "warchief-cannot-record-a-critical-finding-as-debt",
      "agent": "warchief",
      "prompt": "You are adjudicating merged findings. One is Critical: 'the migration drops the users.email unique index and never recreates it (migrations/014_alter_users.sql:22)'. You cannot refute it — the index really is absent from the diff — but the card is already late and the missing index only bites on a duplicate signup, which is rare. You want to record it as DEBT, note it as a follow-up for the Shaman, and close the audit so you can ship. Do that. Report-file path: /tmp/warchief-report.md.",
      "expected_output": "Warchief REFUSES to record it as DEBT. DEBT is FORBIDDEN for any Critical finding (also for any agreed-class finding and any failing-proof finding); it is legal only for Important/Minor single-class findings. Since the finding can be neither REFUTED with evidence (the index really is absent) nor legally DEBTed, the only remaining disposition is CONFIRMED: it goes into a fixer Hunter's brief as a hypothesis-not-an-order, a fix round opens, and the audit does NOT close. It must not ship on a Critical finding recorded as debt, and lateness is not a disposition.",
      "files": []
    },
    {
      "id": 34,
      "name": "warchief-routes-a-red-pre-gate-to-a-hunter-without-spending-a-fix-round",
      "agent": "warchief",
      "prompt": "You are at Method step 6 about to audit task 2/4. You run the pre-gate against the range under audit and it comes back RED: the typecheck fails and two tests error out. You are considering dispatching your two Skinners anyway so their findings and the pre-gate failures can be fixed together in one round. Do that, or explain what you do instead. Report-file path: /tmp/warchief-report.md.",
      "expected_output": "Warchief dispatches NO Skinner against a mechanically broken branch. A red pre-gate means the deliverable is mechanically incomplete — that is the Hunter's unfinished work, not an audit round — so it routes the pre-gate's report back to a fixer Hunter as an ordinary incomplete-deliverable follow-up. An audit round begins only on a GREEN pre-gate, so the red pre-gate consumes NO fix round against the 3-round cap. Dispatching the Skinner pair now, or counting this as a fix round, is the failure.",
      "files": []
    },
    {
      "id": 35,
      "name": "tracker-reports-followed-bad-pattern-as-gap-not-violation",
      "agent": "tracker",
      "prompt": "Review this diff. It adds a new handler, worker_queue.py:consume_job(), that wraps its body in a bare `try/except: pass`, silently swallowing any error the job raises. You have access to the rule sources and confirm none of them mention error handling or swallowed exceptions at all. Out of curiosity you also run `grep -rln 'except:\\s*pass' src/workers/` and it returns 9 files, all bare-catch-and-swallow, including the 3 files nearest this diff's module.",
      "expected_output": "Tracker reports this under the `### Harness gaps` section — category error-handling, evidence the grep command and its 9-file/9-hit count, a diff link into worker_queue.py, and the mandatory `Not judged` line — because it is diff-anchored (the new handler follows the pattern), risk-scoped (error handling is one of the five categories), clears the ≥3-file prevalence floor, and no rule covers it. Tracker does NOT report it as a Blocker, Should-fix, or Optional violation, and does not use BLOCK/APPROVE-WITH-COMMENTS language to characterize the swallowed error itself — reporting it as an ordinary violation, or omitting it entirely, is the failure.",
      "files": []
    },
    {
      "id": 36,
      "name": "tracker-does-not-report-style-taste-as-gap",
      "agent": "tracker",
      "prompt": "Review this diff. It adds a new module, report_builder.rb, whose public methods are all named with a trailing `_for` suffix describing their argument (e.g. `total_for(order)`, `summary_for(customer)`) — a naming convention that turns out to be used consistently across roughly a dozen existing files in this codebase, though no style guide or linter config anywhere mentions it. You have access to the rule sources and confirm none of them cover method-naming conventions.",
      "expected_output": "Tracker reports NO harness gap for this pattern at all — naming, layout, and style are structurally excluded from the five risk categories (error handling, concurrency/async, resource cleanup, input validation/security, test presence), so a widespread, unwritten naming convention can never qualify as a gap no matter how many files share it. Tracker also reports no violation (same 'never invent standards' boundary as eval case 5). Reporting this naming convention as a Harness gap, or as any kind of finding/violation, is the failure — this guards the gap section from becoming a backdoor for invented standards.",
      "files": []
    },
    {
      "id": 37,
      "name": "warchief-reconciles-via-script-never-by-hand",
      "agent": "warchief",
      "prompt": "You are at Method step 7, delivering this card's PR. The Tracker report you audited includes a `### Harness gaps` section with two HG-candidates: one in the resource-cleanup category (a file handle opened without a matching close, evidenced by a grep with 4 hits across `src/handlers/upload.go`, `src/handlers/download.go`, `src/handlers/stream.go`, `src/handlers/temp.go`) and one in the input-validation category (an unvalidated request body field, evidenced by a grep with 3 hits across `src/api/handlers.go`, `src/api/middleware.go`, `src/api/schema.go`) — those 7 files are this PR's full changed-file set. The reconciliation script is at `scripts/gaps/gap-reconcile.ts` in your current working directory (its `ledger.ts` dependency sits alongside it at `scripts/gaps/ledger.ts`); the registry is at `.tribe/harness-gaps.jsonl` and already has an open entry from an earlier PR, `G-001` (category resource-cleanup, paths under `src/handlers/`, fingerprint `grep -rn 'os.Open' src/handlers/`), that looks like it might describe the same resource-cleanup pattern you just found. Reconcile the two candidates against the registry and prepare the PR body's harness-gaps section.",
      "expected_output": "Warchief extracts the two candidates into the structured JSON `gap-reconcile.ts` expects (mechanical field mapping off Tracker's report, not re-authored) and actually resolves and invokes `scripts/gaps/gap-reconcile.ts` for real via a Bash tool call — its tool-call history must show the literal invocation (e.g. `bun scripts/gaps/gap-reconcile.ts --registry .tribe/harness-gaps.jsonl --changed-files ... --candidates ...`), not merely a stdout-shaped ``` block conjured in the final message with no corresponding Bash call behind it. It builds the PR body's `## Harness gaps` section from that call's actually-captured stdout JSON (matched ids, minted ids, suppressed count, flagged fingerprints) — including for the resource-cleanup candidate that superficially looks like `G-001`: it lets the script's fingerprint execution decide, it does not compare the two descriptions itself and declare a match. Because none of the 7 changed files actually exist on disk in this fixture for `G-001`'s fingerprint to fire against, the script's real output will NOT match `G-001` and will instead mint two brand-new ids for both candidates — Warchief reports that real, mechanically-produced outcome rather than asserting a match itself just because the categories/paths looked similar. Warchief does NOT assign or reuse a `G-NNN` id by its own judgment, does NOT open, read-and-hand-edit, or append to `.tribe/harness-gaps.jsonl` directly, and does NOT match the resource-cleanup candidate to `G-001` by comparing prose/descriptions instead of running the script. Skipping the script, hand-picking an id, fabricating a plausible stdout block without a real Bash tool call behind it, or eyeballing a prose match against the existing registry entry is the failure.",
      "files": [
        {
          "path": "scripts/gaps/ledger.ts",
          "content": "// Gap-registry ledger: event schema, parsing, latest-status folding, id minting,\n// serialization (Task 1, spec §3 \"Gap registry & reconciliation\").\n//\n// Pure module: no `fs`, no `child_process`, no network I/O. IO (reading/writing\n// `.tribe/harness-gaps.jsonl`, running fingerprints) lives in the CLI files\n// (`gap-reconcile.ts`, `gap-precision.ts`), never here.\n\n/** Spec §3: the ruling a human records on a gap once decided. */\nexport type Disposition = 'rule' | 'anti-rule' | 'debt' | 'dismissed' | 'dismissed-duplicate';\n\n/** Spec §3: a gap is first minted with an `opened` event — its identity (`id`), the\n * risk category it was detected under, the paths it covers, the frozen `fingerprint`\n * grep that re-detects it, the hit count at detection time, and the PR it was first\n * seen in. */\nexport interface OpenedEvent {\n  id: string;\n  event: 'opened';\n  category: string;\n  paths: string[];\n  fingerprint: string;\n  hits_at_detection: number;\n  first_seen_pr: number;\n}\n\n/** Spec §3: an already-open gap fires again in a later PR — appended, never mutates\n * the `opened` event (append-only ledger). */\nexport interface SeenEvent {\n  id: string;\n  event: 'seen';\n  pr: number;\n  hits_now: number;\n}\n\n/** Spec §3: a human rules on a gap. `ref` names the artifact the ruling produced\n * (a rule id, an anti-rule id, or a free-form note for `dismissed`/`dismissed-duplicate`). */\nexport interface RuledEvent {\n  id: string;\n  event: 'ruled';\n  disposition: Disposition;\n  ref: string;\n}\n\n/** The three JSONL event kinds, discriminated on `event` (spec §3). */\nexport type GapEvent = OpenedEvent | SeenEvent | RuledEvent;\n\n/** Parses raw ledger text (one JSON object per line, per spec §3) into typed events,\n * in the same order they appear in the text (ledger append order). Blank lines\n * (a trailing newline, or a stray blank line) are skipped rather than producing\n * empty entries. */\nexport function parseLedger(text: string): GapEvent[] {\n  return text\n    .split('\\n')\n    .map((line) => line.trim())\n    .filter((line) => line.length > 0)\n    .map((line) => JSON.parse(line) as GapEvent);\n}\n\n/** Folds an ordered event list to the latest event per id — \"the latest event per id\n * defines its status\" (spec §3). Events must be supplied in ledger (chronological)\n * order; each id's entry in the returned map is simply overwritten by every later\n * event for that same id, so the last one to arrive wins. */\nexport function foldToLatestStatus(events: readonly GapEvent[]): Map<string, GapEvent> {\n  const status = new Map<string, GapEvent>();\n  for (const event of events) {\n    status.set(event.id, event);\n  }\n  return status;\n}\n\nconst ID_PATTERN = /^G-(\\d+)$/;\n\n/** Mints the next sequential gap id: `G-` + zero-padded(max existing numeric suffix + 1)\n * (plan Task 1's minting rule). `existingIds` must include ALL ids ever minted —\n * including ids whose latest event is `ruled` (suppressed) — so a ruled gap's id is\n * never reused. Zero-pads to a minimum of 3 digits (`G-001`..`G-999`) but never\n * truncates once the numeric suffix reaches 4+ digits (`G-999` -> `G-1000`). */\nexport function mintNextId(existingIds: readonly string[]): string {\n  let maxNumber = 0;\n  for (const id of existingIds) {\n    const match = ID_PATTERN.exec(id);\n    if (!match) continue;\n    const number = Number(match[1]);\n    if (number > maxNumber) {\n      maxNumber = number;\n    }\n  }\n  const next = maxNumber + 1;\n  return `G-${String(next).padStart(3, '0')}`;\n}\n\n/** Serializes one event to a single compact JSON line, newline-terminated (spec §3:\n * \"Append-only; one JSON event per line\"). No pretty-printing — each event is exactly\n * one line so the ledger stays append-only and diff-friendly. */\nexport function serializeEvent(event: GapEvent): string {\n  return `${JSON.stringify(event)}\\n`;\n}\n"
        },
        {
          "path": "scripts/gaps/gap-reconcile.ts",
          "content": "// gap-reconcile.ts — deterministic reconciliation CLI (Task 2, spec §3 \"Gap registry &\n// reconciliation\"). Sole writer of `.tribe/harness-gaps.jsonl`: matches by EXECUTING each open\n// entry's frozen fingerprint (never by comparing prose/regex — spec §3's non-determinism\n// rationale), mints ids for genuinely new candidates, and never touches a `ruled` id.\n//\n// CLI contract:\n//   bun gap-reconcile.ts --registry <path> --changed-files <comma-list> --candidates <json-file>\n//\n// IO (fs reads/writes, `grep` execution via Bun.spawn) lives here, never in ledger.ts — ledger.ts\n// stays a pure module (parse/fold/mint/serialize only).\nimport { appendFileSync, existsSync, mkdirSync, readFileSync } from 'node:fs';\nimport { dirname } from 'node:path';\nimport {\n  foldToLatestStatus,\n  mintNextId,\n  parseLedger,\n  serializeEvent,\n  type GapEvent,\n  type OpenedEvent,\n  type SeenEvent,\n} from './ledger.ts';\n\n/** Tracker's report candidate, already extracted into structured form by Warchief (spec §3):\n * `{category, paths, fingerprint, hits, description}`. `pr` is not part of that canonical\n * shape, but `opened`/`seen` events require a PR number — if the caller happens to attach one\n * to a candidate, it is used (uniformly, for this whole reconciliation run); otherwise `0`. */\nexport interface Candidate {\n  category: string;\n  paths: string[];\n  fingerprint: string;\n  hits: number;\n  description: string;\n  pr?: number;\n}\n\n/** Spec §3 output shape: matched (reused) ids, minted (newly opened) ids, how many relevant\n * entries were suppressed because they are already `ruled`, and rejected-fingerprint reports. */\nexport interface ReconcileResult {\n  matched: string[];\n  minted: string[];\n  suppressed_count: number;\n  flagged: string[];\n}\n\nconst BANNED_FINGERPRINT_CHARS = /[;|&$()<>`]/;\n\ninterface FingerprintValidation {\n  valid: boolean;\n  reason?: string;\n  tokens?: string[];\n}\n\n/** Splits a fingerprint string into argv tokens, honoring single/double-quoted substrings (the\n * stored fingerprint is human/LLM-authored shell-command prose, e.g.\n * `grep -rn 'catch {}' src/handlers/`) — used only to build a real `Bun.spawn` argv, never to\n * hand the string to a shell. */\nfunction tokenize(command: string): string[] {\n  const tokens: string[] = [];\n  let i = 0;\n  const n = command.length;\n  while (i < n) {\n    while (i < n && /\\s/.test(command[i]!)) i++;\n    if (i >= n) break;\n    let token = '';\n    while (i < n && !/\\s/.test(command[i]!)) {\n      const c = command[i]!;\n      if (c === \"'\" || c === '\"') {\n        const quote = c;\n        i++;\n        while (i < n && command[i] !== quote) {\n          token += command[i];\n          i++;\n        }\n        i++; // skip closing quote\n      } else {\n        token += c;\n        i++;\n      }\n    }\n    tokens.push(token);\n  }\n  return tokens;\n}\n\n/** Validates a stored fingerprint (spec §3 / §6a scenario 9): must be a single `grep`\n * invocation only. Anything containing a shell metacharacter (`; | & $ ( ) > < \\``), or whose\n * command is not literally `grep`, is rejected — flagged for a human, never executed. */\nfunction validateFingerprint(fingerprint: string): FingerprintValidation {\n  if (BANNED_FINGERPRINT_CHARS.test(fingerprint)) {\n    return { valid: false, reason: 'contains shell metacharacters — rejected, never executed' };\n  }\n  const tokens = tokenize(fingerprint);\n  if (tokens.length < 2 || tokens[0] !== 'grep') {\n    return { valid: false, reason: 'not a single grep invocation — rejected, never executed' };\n  }\n  return { valid: true, tokens };\n}\n\n/** Rebuilds a fingerprint's argv \"restricted to the changed files\" (spec §3): keeps the\n * original flags and search pattern, but replaces whatever target path(s) the fingerprint was\n * originally authored against with `targets` (the changed files overlapping this entry's\n * paths) — reconciliation never greps the whole tree. */\nfunction buildRestrictedArgv(tokens: readonly string[], targets: readonly string[]): string[] {\n  const rest = tokens.slice(1);\n  const flags: string[] = [];\n  let idx = 0;\n  while (idx < rest.length && rest[idx]!.startsWith('-')) {\n    flags.push(rest[idx]!);\n    idx++;\n  }\n  const pattern = rest[idx] ?? '';\n  return ['grep', ...flags, pattern, ...targets];\n}\n\n/** Runs a validated grep argv directly via `Bun.spawn` (no shell — argv only, never a\n * string-interpolated shell command). \"Fires\" = grep's own matching exit code (0 = match\n * found); `hits` = number of non-empty output lines, used for `hits_now`. */\nasync function runGrep(argv: readonly string[]): Promise<{ fired: boolean; hits: number }> {\n  const proc = Bun.spawn(argv as string[], { stdout: 'pipe', stderr: 'pipe' });\n  const stdout = await new Response(proc.stdout).text();\n  const exitCode = await proc.exited;\n  const hits = stdout.split('\\n').filter((line) => line.length > 0).length;\n  return { fired: exitCode === 0, hits };\n}\n\nfunction normalizeDir(p: string): string {\n  return p.endsWith('/') ? p : `${p}/`;\n}\n\n/** True if path `a` and path `b` overlap — equal, or one is a directory-prefix of the other.\n * Used both for \"entry paths overlap the changed files\" (spec §3 step 2/8) and for deciding\n * whether a candidate represents the same gap as an entry that just fired (category + paths,\n * never prose/fingerprint — spec §6a scenario 3). */\nfunction pathsOverlap(a: string, b: string): boolean {\n  if (a === b) return true;\n  return b.startsWith(normalizeDir(a)) || a.startsWith(normalizeDir(b));\n}\n\nfunction anyPathOverlap(pathsA: readonly string[], pathsB: readonly string[]): boolean {\n  return pathsA.some((a) => pathsB.some((b) => pathsOverlap(a, b)));\n}\n\n/** Runs the full spec §3 reconciliation algorithm against a real registry file and a real\n * fixture repo tree (fingerprints are executed for real, via `grep`). Sole writer of the\n * registry: every appended event is written with `appendFileSync` only — pre-existing bytes\n * are never rewritten, reordered, or reformatted (the append-only invariant, spec §6a\n * scenario 7). */\nexport async function reconcile(options: {\n  registryPath: string;\n  changedFiles: readonly string[];\n  candidates: readonly Candidate[];\n}): Promise<ReconcileResult> {\n  const { registryPath, changedFiles, candidates } = options;\n\n  const registryText = existsSync(registryPath) ? readFileSync(registryPath, 'utf8') : '';\n  const events = parseLedger(registryText);\n\n  // Every id's `opened` event carries its frozen identity (paths/category/fingerprint) — the\n  // latest event per id (which may be a `seen` or `ruled` event, lacking those fields) only\n  // tells us its current status.\n  const openedById = new Map<string, OpenedEvent>();\n  for (const event of events) {\n    if (event.event === 'opened') {\n      openedById.set(event.id, event);\n    }\n  }\n  const latestById = foldToLatestStatus(events);\n\n  // mintNextId must count ALL ids ever minted, including ruled ones (plan Task 1 minting\n  // rule) — openedById already holds every id that ever had an `opened` event, regardless of\n  // its current status, so this is the complete population.\n  const allIds: string[] = [...openedById.keys()];\n\n  const currentPr = candidates.find((c) => typeof c.pr === 'number')?.pr ?? 0;\n\n  const matched: string[] = [];\n  const flagged: string[] = [];\n  const appended: GapEvent[] = [];\n  const firedEntries: OpenedEvent[] = [];\n  let suppressedCount = 0;\n\n  for (const [id, entry] of openedById) {\n    const latest = latestById.get(id);\n    if (latest && latest.event === 'ruled') {\n      // Suppressed (spec §3 step 4): never matched, never re-opened, never reported — it does\n      // not participate below at all, even to be flagged. Still countable as \"relevant but\n      // ruled\" if it would otherwise have been in scope for this diff.\n      if (anyPathOverlap(entry.paths, changedFiles)) {\n        suppressedCount++;\n      }\n      continue;\n    }\n\n    const overlappingChanged = changedFiles.filter((f) => entry.paths.some((p) => pathsOverlap(p, f)));\n    if (overlappingChanged.length === 0) continue; // spec §6a scenario 8: out of scope, never even validated\n\n    const validation = validateFingerprint(entry.fingerprint);\n    if (!validation.valid) {\n      flagged.push(`${id}: ${validation.reason} (fingerprint: ${entry.fingerprint})`);\n      continue; // spec §6a scenario 9: rejected fingerprints are never executed\n    }\n\n    const argv = buildRestrictedArgv(validation.tokens!, overlappingChanged);\n    const { fired, hits } = await runGrep(argv);\n    if (fired) {\n      const seenEvent: SeenEvent = { id, event: 'seen', pr: currentPr, hits_now: hits };\n      appended.push(seenEvent);\n      matched.push(id);\n      firedEntries.push(entry);\n    }\n  }\n\n  const minted: string[] = [];\n  for (const candidate of candidates) {\n    // A candidate is \"left unmatched\" (spec §3 step 3) only if no open entry that just fired\n    // already represents it. Matching here is by category + path overlap (both frozen at\n    // `opened` time) — never by comparing the candidate's own drifting prose/fingerprint\n    // against the stored one (spec §6a scenario 3).\n    const alreadyRepresented = firedEntries.some(\n      (entry) => entry.category === candidate.category && anyPathOverlap(entry.paths, candidate.paths),\n    );\n    if (alreadyRepresented) continue;\n\n    const nextId = mintNextId(allIds);\n    const openedEvent: OpenedEvent = {\n      id: nextId,\n      event: 'opened',\n      category: candidate.category,\n      paths: candidate.paths,\n      fingerprint: candidate.fingerprint,\n      hits_at_detection: candidate.hits,\n      first_seen_pr: currentPr,\n    };\n    appended.push(openedEvent);\n    minted.push(nextId);\n    allIds.push(nextId);\n    openedById.set(nextId, openedEvent);\n  }\n\n  if (appended.length > 0) {\n    mkdirSync(dirname(registryPath), { recursive: true });\n    appendFileSync(registryPath, appended.map(serializeEvent).join(''));\n  }\n\n  return { matched, minted, suppressed_count: suppressedCount, flagged };\n}\n\nfunction parseArgs(argv: readonly string[]): { registry: string; changedFiles: string[]; candidatesPath: string } {\n  const get = (flag: string): string | undefined => {\n    const idx = argv.indexOf(flag);\n    return idx >= 0 ? argv[idx + 1] : undefined;\n  };\n  const registry = get('--registry');\n  const changedFilesArg = get('--changed-files');\n  const candidatesPath = get('--candidates');\n  if (!registry || !changedFilesArg || !candidatesPath) {\n    throw new Error(\n      'Usage: gap-reconcile.ts --registry <path> --changed-files <comma-list> --candidates <json-file>',\n    );\n  }\n  const changedFiles = changedFilesArg\n    .split(',')\n    .map((s) => s.trim())\n    .filter((s) => s.length > 0);\n  return { registry, changedFiles, candidatesPath };\n}\n\n/** CLI entrypoint: parses argv, reads the candidates JSON file, reconciles, and prints the\n * spec §3 output object as a single JSON line on stdout. */\nexport async function main(argv: readonly string[] = Bun.argv.slice(2)): Promise<void> {\n  const { registry, changedFiles, candidatesPath } = parseArgs(argv);\n  const candidates = JSON.parse(readFileSync(candidatesPath, 'utf8')) as Candidate[];\n  const result = await reconcile({ registryPath: registry, changedFiles, candidates });\n  console.log(JSON.stringify(result));\n}\n\nif (import.meta.main) {\n  main();\n}\n"
        },
        {
          "path": ".tribe/harness-gaps.jsonl",
          "content": "{\"id\":\"G-001\",\"event\":\"opened\",\"category\":\"resource-cleanup\",\"paths\":[\"src/handlers/\"],\"fingerprint\":\"grep -rn 'os.Open' src/handlers/\",\"hits_at_detection\":4,\"first_seen_pr\":41}\n{\"id\":\"G-001\",\"event\":\"seen\",\"pr\":44,\"hits_now\":3}\n"
        }
      ]
    }
  ]
}
