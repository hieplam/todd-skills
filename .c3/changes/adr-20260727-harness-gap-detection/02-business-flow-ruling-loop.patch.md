---
target: c3-215
scope: block
base: c3-215#n1061@v1:sha256:5cab0d63685ee38f3deb8a93272258dee9c326322b000050ced77ead11f6aa7f
---
| Unattended path | One owner directive ("orchestration: do these N ideas") and then no owner intervention until one report. orchestrate-campaign assumes Shaman authority → Stage A authors the specs, plans and campaign state, landing them as a docs PR → the runner loops at zero token cost, verifying each card mechanically, and on an escalation it PARKS that card and continues to the next progressable one (a dependent of a parked card is blocked, never started) → the orchestrator answers within-authority escalations into answers.md and re-triggers, capped at 2 rounds per card → ONE consolidated report: every card either shipped (PR and merge sha, independently re-verified rather than agent-claimed) or blocked (the question, and why it needs the owner). The sole designed interruption is an irreversible decision — data shapes, product promises, new permissions, privacy — which parks for the owner by campaign config | N.A - see plugins/tribe/skills/orchestrate-campaign/SKILL.md, plugins/tribe/scripts/runner/README.md |
| Harness-gap ruling loop | Tracker's diff review surfaces a candidate gap → gap-reconcile.ts opens it (mints `G-NNN`) on first sighting or appends `seen` on repeat, matching by executing the frozen fingerprint, never by comparing prose → until CU-3 lands, Warchief carries every open/new gap into the PR description under a `## Harness gaps` heading as the interim, human-visible sink → a human rules one of `rule` (adopt the pattern as a standard) / `anti-rule` (forbid it) / `debt` (acknowledge, blacklist, refactor later) / `dismissed` / `dismissed-duplicate` (stale-fingerprint duplicate), appended as a `ruled` event that suppresses the id from all future reports → a `rule`/`anti-rule` ruling feeds back into Tracker's normal rule-checking path once written to a rule source Tracker already loads (`~/.claude/rules/`, `CLAUDE.md`, `.claude/rules/`, C3 rules); `debt` feeds CU-3's blacklist countdown (out of scope here); `dismissed`/`dismissed-duplicate` silence the id. Every ruling thickens the harness — the self-improving ratchet the design exists for | N.A - see docs/superpowers/specs/2026-07-27-tribe-harness-gap-detection-design.md §1-3 |
