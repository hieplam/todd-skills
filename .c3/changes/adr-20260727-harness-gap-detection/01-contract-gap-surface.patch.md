---
target: c3-215
scope: block
base: c3-215#n1083@v1:sha256:1a61541efe10a73b572cb707f30b19b51c0639073dbeed30236cc3ea8e54d77b
---
| scripts/migrate-campaign-home.sh | IN | One-shot, idempotent migration: moves a target repo's pre-existing <repo>/.claude/state/<campaign>/reports/*.md worker reports into $(tribe-home.sh <repo>)/campaigns/<campaign>/reports/ (never re-derives the ~/.tribe key itself). Supports --campaign <slug> (default: every campaign found) and --dry-run (print moves, touch nothing). Refuses to overwrite an existing destination file (prints CONFLICT, non-zero exit, continues other campaigns) and refuses to touch a campaign whose .runner.lock is held by a live pid. Does NOT migrate old session logs (caller-chosen via --logs-dir, no deterministic source) — prints a reminder listing every campaign it touched | bash script, repo-invoked (never installed) | plugins/tribe/scripts/tests/test-migrate-campaign-home.sh |
| scripts/gaps/gap-reconcile.ts (gap reconciliation CLI) | IN | Deterministic CLI: `--registry <path> --changed-files <list> --candidates <json-file>`. Sole writer of `.tribe/harness-gaps.jsonl` in the target repo — an append-only ledger, one JSON event per line (`opened` / `seen` / `ruled`), latest event per id defines status. Matches OPEN entries whose paths overlap the changed files by executing their stored fingerprint (single `grep` invocation only, restricted to the changed files; malicious/malformed fingerprints rejected and flagged, never run); mints the next `G-NNN` for unmatched candidates; suppresses entries already `ruled`. No agent ever edits the registry file directly — every identity decision (match, mint, suppress) is this script's, never inferred from prose | bun CLI, repo-invoked (never installed); registry instance lives in the target repo, never in this repo (C3 "instances are CLI-only" boundary) | plugins/tribe/scripts/gaps/gap-reconcile.test.ts |
| scripts/gaps/gap-precision.ts (gap precision metric CLI) | IN | `--registry <path> [--window 20]`: computes precision over the trailing window of `ruled` gaps as `ruled(rule ∪ anti-rule ∪ debt) ÷ ruled(all)`, excluding `dismissed-duplicate` from both sides; still-open gaps never enter the ratio; emits an overall figure plus a per-category breakdown. Computed, never claimed by an agent | bun CLI, repo-invoked (never installed) | plugins/tribe/scripts/gaps/gap-precision.test.ts |
| Tracker harness-gap report | OUT | New `### Harness gaps` section appended to Tracker's per-diff report, after its Checklist: emits candidate gaps only (no `G-NNN` id — identity is assigned downstream by gap-reconcile.ts), capped at ≤3 per review with a mandatory `Not judged` line per gap. Reported only when diff-anchored, in one of five risk categories, prevalence ≥3 files (grep hit count quoted), and no written rule covers the pattern — Tracker stays read-only and stateless; it is the sole source of candidates gap-reconcile.ts reconciles | agent report section | plugins/tribe/agents/tracker.md |
