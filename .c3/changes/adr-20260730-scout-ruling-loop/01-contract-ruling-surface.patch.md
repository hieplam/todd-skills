---
target: c3-215
scope: block
base: c3-215#n1271@v1:sha256:1a61541efe10a73b572cb707f30b19b51c0639073dbeed30236cc3ea8e54d77b
---
| scripts/migrate-campaign-home.sh | IN | One-shot, idempotent migration: moves a target repo's pre-existing <repo>/.claude/state/<campaign>/reports/*.md worker reports into $(tribe-home.sh <repo>)/campaigns/<campaign>/reports/ (never re-derives the ~/.tribe key itself). Supports --campaign <slug> (default: every campaign found) and --dry-run (print moves, touch nothing). Refuses to overwrite an existing destination file (prints CONFLICT, non-zero exit, continues other campaigns) and refuses to touch a campaign whose .runner.lock is held by a live pid. Does NOT migrate old session logs (caller-chosen via --logs-dir, no deterministic source) — prints a reminder listing every campaign it touched | bash script, repo-invoked (never installed) | plugins/tribe/scripts/tests/test-migrate-campaign-home.sh |
| scripts/gaps/gap-rule.ts (ruling CLI) | IN | Sole writer of `ruled` events and debt entities. `--registry <path> --gap <G-NNN> --disposition <rule\|anti-rule\|debt\|dismissed\|dismissed-duplicate> --ratified-by <owner\|shaman> [--ref <rule-id>] [--debt-slug <slug>] [--check <grep-cmd>] [--description <text>] [--c3-bin <cmd>] [--repo <path>]`, five ordered crash-safe steps: (1) fold registry, refuse a gap that is unknown or already `ruled`; (2) `rule`/`anti-rule`/`debt` require `--ref` naming an existing `.c3/rules/<ref>.md` (existence stat only, never parses content); (3) `debt` requires `--check`/`--debt-slug`/`--description`, validated via `validateFingerprint` (shell metacharacters refused before any execution) and executed via a validated `git grep` translation — zero hits or an erroring check is refused, no entity created; (4) snapshot `git status --porcelain -- .c3/` before invoking `<c3-bin> add debt <slug> --file <tmp-body>` + `<c3-bin> set debt-<slug> status open`, re-snapshot after — any changed tracked path other than `.c3/documents/debt/debt-<slug>.md` (gitignored `c3.db` excepted) fails loudly, naming the strays; (5) the `ruled` event (carrying `ratified_by`) is appended only after steps 1-4 succeed, so a crash mid-ruling never leaves an event with no entity. No agent ever hand-edits the registry or a debt entity file directly — every ruling decision is this script's | bun CLI, repo-invoked (never installed) | plugins/tribe/scripts/gaps/gap-rule.test.ts |
| scripts/gaps/debt-count.ts (burn-down gate CLI) | IN | `[--repo <path>] [--ref <tree-ish>] [--diff <base-ref>]` (ref default `HEAD`). Snapshot mode reads open debt entities from a git tree-ish (`debt-tree.ts`'s `listDebtEntities`/`runCheck`, shared with gap-rule.ts's validated-`git grep` execution) and emits `{ref, sha, entries: [{id, baseline, now, flags}], totals}` — `now > baseline` flags `harness-leak`, `now == 0` flags `closable`, closed entities excluded. `--diff` mode compares a base and head tree and emits only nonzero-delta entries with `new_hits`. **STRONG gate: exits 1 iff any delta is positive**, else 0 — the mechanical check a PR cannot argue down | bun CLI, repo-invoked (never installed) | plugins/tribe/scripts/gaps/debt-count.test.ts |
| scripts/gaps/debt-backfill.ts (issue backfill CLI) | IN | `[--repo <path>] [--ref master] [--gh-bin gh] [--dry-run]`. Sole creator of tech-debt tracking issues: reads open entities from `--ref` via `listDebtEntities`, fetches existing issue titles/bodies via `<gh-bin> issue list`, and creates one issue per open entity absent from every issue text (`selectMissing`/`issueBody` pure core, idempotent — a second run selects nothing already covered) — each issue's body carries the check, baseline, anti-rule, origin gap, a `c3 read <id>` pointer, and the fix-protocol line naming the debt-fix workflow. `--gh-bin` pointing at a missing executable is a no-op, exit 0, never a hard failure | bun CLI, repo-invoked (never installed) | plugins/tribe/scripts/gaps/debt-backfill.test.ts |
| Debt entity instances + shipped `debt` canvas | IN | Debt entity instances live at `.c3/documents/debt/debt-<slug>.md` in the *target* repo only (never in this repo), mirroring the campaign-runner and CU-2 registry's own "instances are CLI-only" boundary already in this Contract — created exclusively by `gap-rule.ts`'s step 4, never hand-authored. The canvas definition itself, `plugins/tribe/canvases/debt.md`, ships with the plugin (Meter table: Check/Anti Rule/Origin Gap/Baseline; free-text Description) and Scout self-provisions it into a target repo lacking it via `c3 canvas add debt --file <plugin-root>/canvases/debt.md` before its first `debt` ruling | markdown c3 canvas definition (plugin-shipped) + CLI-created instances (target-repo-only) | plugins/tribe/canvases/debt.md, plugins/tribe/scripts/gaps/debt-entity.test.ts |
