---
target: c3-215
scope: block
base: c3-215#n1249@v1:sha256:5cab0d63685ee38f3deb8a93272258dee9c326322b000050ced77ead11f6aa7f
---
| Unattended path | One owner directive ("orchestration: do these N ideas") and then no owner intervention until one report. orchestrate-campaign assumes Shaman authority → Stage A authors the specs, plans and campaign state, landing them as a docs PR → the runner loops at zero token cost, verifying each card mechanically, and on an escalation it PARKS that card and continues to the next progressable one (a dependent of a parked card is blocked, never started) → the orchestrator answers within-authority escalations into answers.md and re-triggers, capped at 2 rounds per card → ONE consolidated report: every card either shipped (PR and merge sha, independently re-verified rather than agent-claimed) or blocked (the question, and why it needs the owner). The sole designed interruption is an irreversible decision — data shapes, product promises, new permissions, privacy — which parks for the owner by campaign config | N.A - see plugins/tribe/skills/orchestrate-campaign/SKILL.md, plugins/tribe/scripts/runner/README.md |
| Scout ruling loop, closed | `gap-reconcile.ts` opens/sees a gap (unchanged) → Scout adjudicates it into a disposition proposal (`rule` / `anti-rule` / `debt` / `dismissed` / `dismissed-duplicate`, drafting rule content or a debt check+description where applicable) → an attended session lets the owner rule directly; an unattended campaign escalates the proposal set to the Shaman for ratification instead — Scout never self-ratifies and never contacts the owner → only after ratification does Scout execute `gap-rule.ts` with `--ratified-by owner\|shaman`, now the sole ruling writer (supersedes this row's prior interim state, "a human reads the PR body") → a ratified `rule`/`anti-rule` feeds Tracker's normal rule-checking path once written to a rule source it already loads; a ratified `debt` entry becomes a blacklist entity Tracker grandfathers — a pre-existing occurrence inside the entity's recorded scope earns one non-blocking `tracked in <debt-id>` note, a new occurrence stays an ordinary anti-rule violation — while `debt-count.ts --diff <merge-base>` blocks any PR that grows a debt entry's mechanical hit count (Warchief routes the listed `new_hits` back to a Hunter, never argues the check down) and `debt-backfill.ts` opens one tracking issue per open entity post-merge → the entity closes (`c3 set <id> status closed`) once its count reaches zero, completing the burn-down the owner's target image describes | N.A - see docs/superpowers/specs/2026-07-29-tribe-scout-ruling-loop-design.md §1-8, docs/superpowers/plans/2026-07-30-tribe-scout-ruling-loop.md |
